#include "Binary_tree.h"

void Graph_lib::Binary_tree::draw() const
{
	for (int i = 1; i <= l; ++i)
	{

	}
}

// If level is zero - do nothing
// If level is one
	// Create a Vector_ref of nodes
	// Create a Node
	// Push node onto Vector_ref
// If level is two or greater
	// Create two nodes
	// Parent node should be Vector_ref -1 and -2
	// Push nodes onto Vector_ref

// Doubly linked nodes?